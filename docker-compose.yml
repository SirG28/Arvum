services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: arvum
      POSTGRES_PASSWORD: arvum
      POSTGRES_DB: arvum
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
