import { BookingListSkeleton } from "@/features/bookings/components/BookingListSkeleton";

export default function ReservationsLoading() {
  return <BookingListSkeleton label="Carregando minhas reservas" />;
}
