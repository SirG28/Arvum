import { BookingDetailSkeleton } from "@/features/bookings/components/BookingDetailSkeleton";

export default function ReceivedBookingDetailLoading() {
  return <BookingDetailSkeleton label="Carregando solicitação" />;
}
